APP_NAME = 'c3loc transports'

# Set to true to make more verbose and simplifies debugging
DEBUG = False
# if set to true, html assets (js,css) will not be monified and concated
ASSETS_DEBUG = False

# used to encrypt login cookies
SECRET_KEY = 'YOURSECRETKEYHERE'

# mus be writeable for the user running flask
UPLOAD_DIR = 'uploads'

# database setup
SQLALCHEMY_DATABASE_URI = 'sqlite:///app.db'
SQLALCHEMY_TRACK_MODIFICATIONS = False


# mail server config
MAIL_SERVER = 'SMTP_HOST'
MAIL_PORT = 587
MAIL_USE_TLS = True
MAIL_USE_SSL = False
MAIL_USERNAME = 'SMTP_USER'
MAIL_PASSWORD = 'SMTP_PASS'
MAIL_DEFAULT_SENDER = 'no-reply@YOURDOMAIN.de'


# zammad integration
ZAMMAD_HOST = 'zammad host'
ZAMMAD_USER = 'user'
ZAMMAD_PASS = 'pass'
ZAMMAD_SECURE = True
ZAMMAD_GROUP_ID = 1